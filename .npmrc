# Only install versions published more than 7 days ago: a compromised publish
# is usually caught and pulled well inside that window.
min-release-age=7

# min-release-age needs npm >= 11.10 (see engines.npm). Older npm accepts the
# key and silently ignores it, so refuse to install rather than pretend.
engine-strict=true
