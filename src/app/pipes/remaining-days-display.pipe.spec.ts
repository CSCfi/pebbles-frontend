import { RemainingDaysDisplayPipe } from './remaining-days-display.pipe';

describe('LeftTimeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingDaysDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
